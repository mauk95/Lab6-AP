The code consists of a file Listing.java which contains the directory structure
which consists of a list of filers/sub folderes a done Boolean which tells if there
are no more directiories to be added. and a size variable.

the program is multithreaded as the crawling, indexing and searching operations 
are done synchrounously.

Github link: https://github.com/mauk95/Lab6-AP/upload/master